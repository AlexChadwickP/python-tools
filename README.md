# python-tools
A collection of tools written in Python to fix command line stuff mainly

## How to Use
In order to use add the folder to your PATH, make sure python3 and pip3 are installed and once you have refreshed your terminal you can 

## Dependencies
Use pip to install the following dependencies
* *pyls* will require `termcolor`

## Known Issues
* *pyls* won't work correctly with the windows command line, instead install `Windows Terminal` from the Windows Store